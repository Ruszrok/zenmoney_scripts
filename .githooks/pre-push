#!/usr/bin/env bash
# All quality checks run before pushing.
set -euo pipefail

echo "[pre-push] typecheck (tsc --noEmit)…"
bun run typecheck
echo "[pre-push] tests (bun test ./src/*.test.ts)…"
bun run test
echo "[pre-push] OK"
